# Repo Workflow

- Make atomic commits only.
- Keep each commit narrowly scoped to one logical change.
- After every commit, push immediately with `git push`.
- If push fails, report the failure before making unrelated changes.
- Do not bundle unrelated fixes into one commit.
